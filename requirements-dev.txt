# Test/dev dependencies. Installs Home Assistant, pytest, and the async/test
# helpers via pytest-homeassistant-custom-component, plus the integration's
# own runtime requirements.
pytest-homeassistant-custom-component
curl_cffi>=0.7.0
pydantic>=2.0
